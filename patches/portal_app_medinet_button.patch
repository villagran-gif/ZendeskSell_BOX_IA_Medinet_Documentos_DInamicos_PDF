*** Begin Patch
*** Update File: public/app.js
@@
 document.addEventListener('DOMContentLoaded', loadPipelinesForDealForm);
 
 document.addEventListener('DOMContentLoaded', () => {
   enforceHeightFormat(document.getElementById('dealEstatura'));
 });
+
+// Botón: Subir a Medinet (se habilita tras "Trato creado")
+document.addEventListener('DOMContentLoaded', () => {
+  const btn = document.getElementById('btnDealToMedinet');
+  if (!btn) return;
+  btn.disabled = true;
+
+  btn.addEventListener('click', async () => {
+    if (!__lastMedinetPayload) {
+      setStatus(dStatus, 'Primero crea el trato. (No hay payload para Medinet).', 'error');
+      return;
+    }
+
+    btn.disabled = true;
+    btn.textContent = 'Subiendo...';
+    setStatus(dStatus, 'Subiendo payload a Medinet...', 'running');
+
+    try {
+      const result = await medinetStorePayload(__lastMedinetPayload);
+      const picked = pickBackendResult(result.json);
+      const msg = picked.message || 'Listo ✅ (payload guardado)';
+      setStatus(dStatus, msg, 'ok');
+
+      if (picked.download_url) {
+        // Mostrar link arriba en resumen
+        dSummary.insertAdjacentHTML('beforeend',
+          `<div class="summary-row"><b>Medinet:</b> <a href="${picked.download_url}" target="_blank" rel="noreferrer">Abrir (mf_key)</a> <span class="muted small">${picked.key ? ('· ' + picked.key) : ''}</span></div>`
+        );
+      }
+
+      // Debug
+      try {
+        dDetails.open = true;
+        dOut.textContent = JSON.stringify({
+          timestamp: new Date().toISOString(),
+          payload: __lastMedinetPayload,
+          backend_response: picked.raw,
+          settings: { portal_proxy: true, import_endpoint: MEDINET_IMPORT_ENDPOINT, used_endpoint: result.endpoint }
+        }, null, 2);
+      } catch (_e) {}
+
+      btn.textContent = 'Listo ✅';
+    } catch (e) {
+      setStatus(dStatus, `Error subiendo a Medinet: ${e && e.message ? e.message : String(e)}`, 'error');
+      btn.textContent = 'Subir a Medinet';
+      btn.disabled = false;
+      return;
+    }
+
+    // dejar el botón activo por si quieren reintentar
+    btn.disabled = false;
+    btn.textContent = 'Subir a Medinet';
+  });
+});
*** End Patch
